---
name: donegraph-dashboard
description: Build and open the local DoneGraph dashboard for the current project.
argument-hint: ["[--no-open]"]
---

# /donegraph-dashboard

Build the current DoneGraph artifacts and open `.donegraph/dashboard.html`.

Run:

```bash
$HOME/.donegraph-plugin/scripts/donegraph dashboard $ARGUMENTS
```

Use `--no-open` when the user only wants to generate the HTML without opening a browser.
